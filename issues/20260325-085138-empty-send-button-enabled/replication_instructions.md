1. Open `http://localhost:$PORT/#70fd21d1-cd91-406c-8ab6-a83785c0fc2e` in a mobile viewport (`390x844`).
2. Wait for the session to finish loading.
3. Leave the composer textarea empty.
4. Inspect the primary `Send` button.
5. The bug is present if the textarea is empty and the `Send` button is still enabled.
