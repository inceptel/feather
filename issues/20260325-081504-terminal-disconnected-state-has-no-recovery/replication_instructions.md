1. Open `http://localhost:$PORT/` on a mobile viewport such as `390x844`.
2. Open the session drawer with the hamburger button.
3. Select any visible session from the list.
4. Switch from `Chat` to `Terminal`.
5. Observe that the terminal view renders only `[disconnected]`.
6. Confirm there is no dedicated terminal recovery control such as `Retry`, `Reconnect`, or `Return to chat`; only the global UI controls remain visible.
